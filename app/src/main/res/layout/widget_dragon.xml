<?xml version="1.0" encoding="utf-8"?>
<!-- Carré 2x2. Le fond est une image peinte, pas une forme XML, donc il porte les
     écailles et change de couleur avec l'humeur. Tout le texte est centré sous le
     dragon, du plus gros au plus fin : la série, ce que dit le dragon, la phrase du
     jour. Aucun bouton — noter une dose se fait dans l'app. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/widget_bg"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY"
        android:contentDescription="@null" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:paddingStart="10dp"
        android:paddingEnd="10dp"
        android:paddingTop="8dp"
        android:paddingBottom="10dp">

        <FrameLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content">

            <ImageView
                android:id="@+id/widget_dragon"
                android:layout_width="72dp"
                android:layout_height="72dp"
                android:contentDescription="Framboise" />

            <TextView
                android:id="@+id/widget_streak"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="top|end"
                android:minWidth="26dp"
                android:gravity="center"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"
                android:paddingTop="2dp"
                android:paddingBottom="3dp"
                android:background="@drawable/widget_streak_bg"
                android:textColor="#FFFFFFFF"
                android:textSize="13sp"
                android:textStyle="bold"
                android:text="12" />
        </FrameLayout>

        <TextView
            android:id="@+id/widget_mood"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:gravity="center"
            android:maxLines="2"
            android:ellipsize="end"
            android:textColor="#FF3B1327"
            android:textSize="12sp"
            android:textStyle="bold"
            android:shadowColor="#40FFFFFF"
            android:shadowDx="0"
            android:shadowDy="1"
            android:shadowRadius="2"
            android:text="Une dose t'attend." />

        <TextView
            android:id="@+id/widget_line"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:gravity="center"
            android:maxLines="2"
            android:ellipsize="end"
            android:textColor="#CC5B2B44"
            android:textSize="10sp"
            android:text="" />
    </LinearLayout>
</FrameLayout>
